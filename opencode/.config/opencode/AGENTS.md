Use the `tavily_search` or `tavily_extract` MCP tools to fetch current documentation
whenever asked about a library, framework, SDK, API, CLI tool, or cloud service.
Prefer official docs pages over general search results.
Use even when you think you know the answer — training data may be outdated.

Do not use for: refactoring, business logic, code review, or general programming concepts.
